# unified-app-connector-platform-167255-167264

This workspace contains the Next.js frontend for the unified connector platform.

- Frontend: connector_frontend (Next.js 14)
- Backend: see sibling container workspace (connector_backend_api) per work item

To run the frontend:
1. cd connector_frontend
2. cp .env.example .env and adjust values
3. npm install
4. npm run dev