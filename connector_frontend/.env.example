# REQUIRED: FastAPI backend base URL (or relative if reverse proxied)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: OAuth redirect/callback URL base used in constructing provider flows
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional feature flags
NEXT_PUBLIC_ENABLE_CHAT_TYPEAHEAD=true
